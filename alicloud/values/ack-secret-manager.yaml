fullnameOverride: ""

command:
  region: "${region}"
  pollingInterval: 24h

rrsa:
  enable: false

image:
  repository: registry-${region}-vpc.ack.aliyuncs.com/acs/ack-secret-manager

cleanupImage:
  repository: registry-${region}-vpc.ack.aliyuncs.com/acs/ack-secret-manager-cleanup
